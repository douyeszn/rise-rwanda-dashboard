[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m pip install -r dashboard/backend/requirements.txt"]

[start]
cmd = "cd dashboard/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
